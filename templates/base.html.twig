<!DOCTYPE html>
<html lang="pl">
    <head>
        <title>{% block title %}Alcomparison - The Alcohol Comparison Website{% endblock %}</title>
        <meta charset="UTF-8">
        <meta name="description" content="You can comparise two different alkohols and choose the best of them.">
        <meta name="Keywords" content="alcomparison, alkohol, comparison, whisky, whiskey, vine, vodka, tequila, beer">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="author" content="Damian Chojnacki">

        <link rel="shortcut icon" href="{{ asset('images/favicon.ico') }}">
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('styles/reset.css') }}">
            <link rel="stylesheet" href="{{ asset('styles/style.css') }}">
        {% endblock %}
    </head>
    <body>
        <!-- ładowanie się strony -->
        <div id="loading" class="loading"></div>
        <!-- tu można wyłączyc -->
        <script>
            document.getElementById("loading").style.display="block";
        </script>

        {% for label, messages in app.flashes %}
            <div class="przyciemnienie">
                {% for message in messages %}
                    <div class="flash-{{ label }}">
                        <div class="flash-{{ label }}__header">
                            Uwaga!
                        </div>

                        <div class="flash-{{ label }}__content">
                            {{ message|trans(messages, 'security') }}
                        </div>

                        <div class="flash-{{ label }}__footer">
                            <button id="flashClose" class="flash-{{ label }}__close">Zamknij</button>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endfor %}

        {% block body %}{% endblock %}
        {% block javascripts %}
            <script src="{{ asset('scripts/jquery.js') }}"></script>
            <script src="{{ asset('scripts/loader.js') }}"></script>
            <script src="{{ asset('scripts/flash.js') }}"></script>
        {% endblock %}
    </body>
</html>
